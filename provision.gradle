import com.seitenbau.ploy.install.config.*
import com.seitenbau.ploy.install.tasks.*
import com.seitenbau.ploy.install.shell.*

task startTomcat(type: ShellCommands) {
  phase( StartTask )
  sudo("/etc/init.d/tomcat7","start")
}
